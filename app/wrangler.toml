name = "skytools"

account_id = "${CLOUDFLARE_ACCOUNT_ID}"
workers_dev = true
compatibility_date = "2024-09-23"
minify = true
compatibility_flags = ["nodejs_compat"]


# Enable the Worker
main = "dist/server/worker.js"


# Only include necessary node_modules for Workers
[build]

#
# Define environment variables
[vars]
NODE_ENV = "production"
